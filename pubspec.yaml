name: freshcut_test
description: A new Flutter project.

# Prevent accidental publishing to pub.dev.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.16.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # ui packages
  gap: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # lints
  very_good_analysis: ^2.4.0
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    # Add assets from the images directory to the application.
    - assets/images/

  fonts:
    # Add fonts to the application.
    - family: Grotesk
      fonts:
        - asset: assets/fonts/AktivGrotesk-Regular.ttf
          weight: 400
        - asset: assets/fonts/AktivGrotesk-Bold.ttf
          weight: 700
